[package]
name = "metric_engine"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
arc-swap = "1.9"
postgres = { version = "0.19.14", optional = true }
tracing = "0.1"

[features]
timescaledb = ["dep:postgres"]
